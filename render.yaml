services:
  - type: web
    name: load-tube
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    region: oregon
    healthCheckPath: /api/health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: load-tube-db
          property: connectionString
      - key: NODE_ENV
        value: production
      - key: PORT
        value: '4000'
      - key: TEMP_DIR
        value: /app/temp
      - key: JWT_SECRET
        generateValue: true
      - key: GOOGLE_CLIENT_ID
        sync: false
      - key: ADMIN_EMAIL
        sync: false
      - key: GMAIL_USER
        sync: false
      - key: GMAIL_APP_PASSWORD
        sync: false
      - key: CORS_ORIGIN
        sync: false

databases:
  - name: load-tube-db
    databaseName: load_tube
    plan: free
